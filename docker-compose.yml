# This is the file Hostinger's Compose-from-URL deploy path fetches directly (SPEC.md §9) —
# it never clones the surrounding repo, so `app.build.context` must be a git URL, not `.`.
# For local development, docker-compose.override.yml overrides this to build from the local tree.

services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    environment:
      SITE_ADDRESS: ${SITE_ADDRESS}
    depends_on:
      - app

  app:
    build:
      # TODO: replace <owner>/<repo> once this repo has a GitHub remote (SPEC.md §9, Phase 0).
      context: https://github.com/<owner>/<repo>.git#main
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      SPOTIFY_CLIENT_ID: ${SPOTIFY_CLIENT_ID}
      SPOTIFY_CLIENT_SECRET: ${SPOTIFY_CLIENT_SECRET}
      SPOTIFY_REDIRECT_URI: ${SPOTIFY_REDIRECT_URI}
      TOKEN_ENCRYPTION_KEY: ${TOKEN_ENCRYPTION_KEY}
      SESSION_SECRET_KEY: ${SESSION_SECRET_KEY}
      DATABASE_PATH: /data/app.db
      DOMAIN: ${DOMAIN}
    volumes:
      - app_data:/data
    expose:
      - "8000"

volumes:
  caddy_data:
  caddy_config:
  app_data:
